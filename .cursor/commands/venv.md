# fish

This will launch the "fish" shell, that I normally use on my Linux machine

This command will be avilable in the chat with "fish"

# venv

This will create a python virtual environment

This command will be available in chat with "venv" (only when fish shell is running)
